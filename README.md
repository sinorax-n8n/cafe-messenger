# 카페 메신저

네이버 및 다음 카페 회원들에게 쪽지를 발송하는 Windows 데스크톱 애플리케이션입니다.

## 기술 스택

[![Built with Claude Code](https://img.shields.io/badge/Built%20with-Claude%20Code-blueviolet?logo=anthropic)](https://claude.ai/code)

- **Electron** - 데스크톱 애플리케이션 프레임워크
- **Vite + TailwindCSS** - 번들링 및 스타일링
- **SQLite** - 로컬 데이터 저장
- **Docker** - 개발 및 빌드 환경
- **Claude Code** - AI 페어 프로그래밍

## 주요 기능

- **네이버 및 다음 계정 관리**: 다중 계정 지원, 비밀번호 AES-256-CBC 암호화 저장
- **카페 링크 관리**: 여러 카페 등록 및 활성/비활성 상태 관리
- **쪽지 템플릿**: 자주 사용하는 메시지 템플릿 저장 및 관리
- **회원 크롤링**: API 기반 카페 회원 자동 수집
- **대량 발송**: BrowserWindow 기반 쪽지 발송, 5~6초 딜레이로 안전하게 발송
- **CAPTCHA 감지**: CAPTCHA 발생 시 시스템 알림 및 UI 경고
- **발송 현황 동기화**: 네이버 서버와 일일 발송량 동기화
- **발송 제외 목록**: 특정 회원 발송 제외 관리

## 스크린샷

> 📷 스크린샷 추가 예정

## 설치 방법

### 사전 요구사항

- Docker Desktop

### 개발 환경 실행

```bash
# 저장소 클론
git clone <repository-url>
cd cafe-messenger

# 핫 리로드가 포함된 개발 서버 시작
docker-compose up desktop-dev

빌드 결과물은 `artifacts/make/` 디렉토리에 생성됩니다.
```

## 사용법

1. **계정 관리** → 네이버/다음 계정 등록
2. **카페 관리** → 대상 카페 URL 등록
3. **템플릿 관리** → 쪽지 템플릿 작성
4. **홈** → 로그인 → 탐색 시작 → 발송 시작


## 프로젝트 구조

```
cafe-messenger/
├── apps/desktop/                    # Electron 애플리케이션
│   ├── src/
│   │   ├── main/                    # Main 프로세스
│   │   │   ├── main.js              # 진입점
│   │   │   ├── ipc/                 # IPC 핸들러
│   │   │   │   ├── account-handler.js   # 계정 CRUD
│   │   │   │   ├── cafe-handler.js      # 카페 CRUD
│   │   │   │   ├── template-handler.js  # 템플릿 CRUD
│   │   │   │   ├── member-handler.js    # 회원 CRUD
│   │   │   │   └── naver-handler.js     # 네이버 연동
│   │   │   └── store/               # 데이터 저장소
│   │   │       ├── index.js         # SQLite 저장소
│   │   │       └── schema.js        # 테이블 스키마
│   │   ├── preload/
│   │   │   └── preload.js           # contextBridge
│   │   └── renderer/
│   │       ├── index.html           # HTML 셸
│   │       ├── renderer.js          # 앱 초기화
│   │       └── components/          # UI 컴포넌트
│   ├── package.json
│   ├── forge.config.js              # Electron Forge 설정
│   └── Dockerfile.dev               # 개발 컨테이너
├── artifacts/                       # 빌드 산출물
├── docker-compose.yml               # Docker 설정
└── CLAUDE.md                        # 개발 가이드
```
